# 配置模型

状态：**has_unresolved** · 1 finding

- P1：[配置缺少版本、验证与原子提交 owner](../issues/configuration-aggregate-missing.md)

`AppConfig` 已经包含通信、设备、GPS、地图、网络、隐私、路线和 APRS 设置，但没有统一的版本、迁移、跨字段验证和原子提交边界。它是“数据存在、模型未形成”，不能被展示成 confirmed Configuration Model。
